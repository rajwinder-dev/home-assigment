services:
  redis:
    image: redis:alpine
    restart: always
    volumes:
      - redis-data:/data
    networks:
      - prisma-network

  api:
    build:
      context: .
      target: api
    image: rajwindersxxx/employee-management:api
    restart: always
    env_file:
      - .env
    environment:
      - PORT=3000
      - REDIS_PORT=6379
      - REDIS_HOST=redis
    command: sh -c " pnpm run start"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "require(\"http\").get(\"http://localhost:3000/health\", (r) => process.exit(r.statusCode === 200 ? 0 : 1))"',
        ]
      retries: 5
    depends_on:
      redis:
        condition: service_started
    networks:
      - prisma-network

  email-worker:
    image: rajwindersxxx/ticket-management:email-worker
    restart: always
    env_file:
      - .env
    environment:
      - REDIS_PORT=6379
      - REDIS_HOST=redis
    depends_on:
      redis:
        condition: service_started
    networks:
      - prisma-network # Essential so it can see the redis container

  web:
    build:
      context: .
      target: web
    image: rajwindersxxx/employee-management:web
    restart: always
    ports:
      - "8081:80"
    depends_on:
      api:
        condition: service_healthy
    networks:
      - prisma-network

networks:
  prisma-network:

volumes:
  redis-data:
